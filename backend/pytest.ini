[pytest]
pythonpath = .
testpaths = tests
markers =
    unit: fast tests that do not require external services
    integration: tests that require PostgreSQL or another service
    concurrency: transaction and race-condition tests using independent sessions
